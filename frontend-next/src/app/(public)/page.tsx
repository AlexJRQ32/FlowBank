import styles from "./page.module.scss";

export default function LandingPage() {
  return (
    <main>
      <h1>FlowBank</h1>
      <p className={styles["token-test"]}>
        Landing placeholder — port from features/landing (Phase 4a).
      </p>
    </main>
  );
}
